# https://medium.com/@shakyShane/laravel-docker-part-1-setup-for-development-e3daaefaf3c?token=i6JtSuvpAr7sbCvu
version: '2'
services:

  # The Application
  app:
    build:
      context: ./
      dockerfile: app.dockerfile
    working_dir: /var/www
    volumes:
      - ./:/var/www
    environment:
      - "DB_PORT=3306"
      - "APP_DEBUG=TRUE"
      - "APP_URL=http://localhost:8080/"
      - "API_URL=https://endp.achadosepedidos.org.br/api/"
      - "SECURITY_SALT=dasfewrvuiofngfdsadsadsadasavunquy32r7831287413"
      - "MYSQL_HOST=database"
      - "MYSQL_USERNAME=root"
      - "MYSQL_PASSWORD=secret"
      - "MYSQL_DATABASE=achadosepedidos"      
      - "MAIL_DRIVER=Smtp"
      - "MAIL_HOST="
      - "MAIL_USERNAME="
      - "MAIL_PASSWORD="   
      - "MYSQL_ATTR_SSL_CA="
      - "BLIPCHAT_KEY=YWNoYWRvc2VwZWRpZG9zOjllMmYzM2FlLWFhOGItNDliMC1iNjNiLWFmODIwMjEyNDE0Yg=="

  # The Web Server
  web:
    build:
      context: ./
      dockerfile: web.dockerfile
    working_dir: /var/www
    volumes_from:
      - app
    ports:
      - 8080:80

  # The Database
  database:
    image: mysql:5.6
    volumes:
      - dbdata:/var/lib/mysql
    environment:
      - "MYSQL_DATABASE=achadosepedidos"
      - "MYSQL_HOST=database"
      - "MYSQL_ROOT_PASSWORD=secret"
    ports:
        - "33061:3306"

volumes:
  dbdata: